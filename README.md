# Neural Network from Scratch

Basic Jupyter notebook that implements a neural network from scratch using only numpy and pandas.

Currently testing on MNIST dataset using a hidden ReLU layer. Will generalize soon.
